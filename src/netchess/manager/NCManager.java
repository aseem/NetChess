package netchess.manager;

public class NCManager 
{
	// create board
	// create player 1
	// create player 2
	
	/* keep track of:
	 * 
	 * who's turn is it?
	 * is the game over?
	 * what's the score?
	 * is someone in check?
	 * output the status of the game at each turn
	 */
}
